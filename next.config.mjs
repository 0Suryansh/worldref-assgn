/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
        domains: ['lh3.googleusercontent.com', 'successive.cloud', 'play-lh.googleusercontent.com', 'encrypted-tbn0.gstatic.com', 'imgv3.fotor.com', 'picsum.photos'],
      },
    
    
};

export default nextConfig;
